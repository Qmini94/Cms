server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: cms

  profiles:
    active: local  # ?? ???? ?? (local, dev, prod ?)

  thymeleaf:
    enabled: true
    prefix: classpath:/templates/view/
    suffix: .html
    mode: HTML
    cache: false

  mvc:
    static-path-pattern: /static/**  # ?? ?? ?? ??
    cors:
      allowed-origins: "https://cms.itid.co.kr"  # ????? ??
      allowed-methods: "GET, POST, PUT, DELETE"
      allowed-headers: "*"  # ?? ?? ??
      allow-credentials: true  # ??, ?? ?? ?? ??

  security:
    user:
      name: admin
      password: admin  # ?? ??? (?? ????? ?? ??)

  session:
    timeout: 30m  # ?? ?? ?? (30?)
    cookie:
      http-only: true  # XSS ?? ??
      secure: true  # HTTPS ????? ?? ?? (?? ? ??)

#  datasource:
#    url: jdbc:mariadb://127.0.0.1:3306/EGOV_CSERVER?serverTimezone=Asia/Seoul
#    username: itid
#    password: Yubi2025!
#    driver-class-name: org.mariadb.jdbc.Driver
#    hikari:
#      maximum-pool-size: 10
#      minimum-idle: 2
#      idle-timeout: 30000
#      max-lifetime: 1800000
#      connection-timeout: 30000
#
#  jpa:
#    database: mysql
#    database-platform: org.hibernate.dialect.MariaDBDialect
#    hibernate:
#      ddl-auto: update
#      naming:
#        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
#    properties:
#      hibernate:
#        format_sql: true
#        show_sql: true
#        jdbc:
#          time_zone: Asia/Seoul

jwt:
  secret: "mySuperSecretKeyForJwtTokenMustBeLongEnough"
  expiration: 3600000  # 1?? (??? ??)
  header: Authorization  # HTTP ???
  prefix: Bearer  # ?? ?? ?? ??? (?: Bearer eyJhbGciOiJIUzI1NiIsInR...)
